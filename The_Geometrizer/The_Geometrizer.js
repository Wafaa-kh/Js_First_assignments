function calcCircumfrence (radius){
    var w = (Math.PI * radius) * 2;
    consol.log("The circumference is" + w);
    }
    
    function calcArea(radius) {
    var m = (Math.PI * radius) * radius;
    console.log("The area is" + m);
    }
    calcCircumfrence (20);
    calcArea(10);